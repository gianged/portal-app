//! Placeholder for upload-maintenance jobs (e.g. sweeping orphaned files under
//! `STORAGE_ROOT` no longer referenced by any attachment or avatar). Not yet
//! implemented: it needs a `FileStorage` listing capability and an orphan
//! policy. Wire a worker into the `Monitor` here once that work is scoped.
